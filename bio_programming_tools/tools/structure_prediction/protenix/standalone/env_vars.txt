# Environment variables for Protenix (protein structure prediction)
#
# Lines starting with # are comments and will be ignored.
# Variables listed under [passthrough] are inherited from the parent environment.
# Variables under [set] are set to specific values in the subprocess.

[passthrough]
# Optional: override Protenix root directory for model downloads
# If not set, uses default location managed by Protenix
PROTENIX_ROOT_DIR
